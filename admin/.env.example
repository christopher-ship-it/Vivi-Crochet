# VIVI Admin — environment configuration
# Copy to .env.local for local development.

# --- Local development ---
VITE_API_BASE_URL=http://localhost:5080

# --- Production build ---
# Values live in .env.production (committed; no secrets).
# VITE_API_BASE_URL=https://app-vivi-api-hwbmc3dzhkewa5hb.centralindia-01.azurewebsites.net
#
# Build: npm run build  (loads .env.production automatically)
# Or:    .\scripts\deploy-admin-azure.ps1 -BuildOnly
#
# Custom domain: https://admin.vivicrochet01.com (API: https://api.vivicrochet01.com)
# SPA routing: public/staticwebapp.config.json (SWA) or public/web.config (App Service)
